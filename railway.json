{
    "$schema": "https://railway.app/railway.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "iopaint start --model=lama --device=cpu --host=0.0.0.0 --port=8080",
        "restartPolicyType": "ON_FAILURE"
    }
}