{
    "$schema": "https://railway.app/railway.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "iopaint start --model=lama --device=cpu --host=0.0.0.0 --port=$PORT",
        "restartPolicyType": "ON_FAILURE"
    }
}